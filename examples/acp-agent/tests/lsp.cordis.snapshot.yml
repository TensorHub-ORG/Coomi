# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@coomi/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@coomi/coomi-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: '@coomi/coomi-lsp'
          - id: lsp-stdio
            name: '@coomi/coomi-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@coomi/coomi-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@coomi/coomi-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@coomi/coomi-llm-replay'
