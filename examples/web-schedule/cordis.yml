# Opt-in Schedule patch over the shipped Web composition. The owner observes
# only roots published after this overlay loads.

- insert:
    - id: time-context
      name: '@coomi/coomi-time-context'

    - id: schedule
      name: '@coomi/coomi-schedule'
