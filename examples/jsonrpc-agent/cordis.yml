# Unattended coding-agent deployment for the bundled coomi-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@coomi/coomi-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.COOMI_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.COOMI_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@coomi/coomi-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@coomi/coomi-subprocess-local'

- id: bash
  name: '@coomi/coomi-bash-local'
  config:
    cwd: !!js process.env.COOMI_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@coomi/coomi-agent-spine-demo'
  config:
    persona: !!js process.env.COOMI_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@coomi/coomi-session-persistence-jsonl'
  config:
    root: !!js process.env.COOMI_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.COOMI_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@coomi/coomi-session-checkpoint-policy'

- id: subagent
  name: '@coomi/coomi-subagent'

- id: subagent-spawn-in-process
  name: '@coomi/coomi-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@coomi/coomi-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@coomi/coomi-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@coomi/coomi-fs-local'
  config:
    cwd: !!js process.env.COOMI_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@coomi/coomi-fs-observation-policy'

- id: tool-fs
  name: '@coomi/coomi-tool-fs'

- id: token-meter
  name: '@coomi/coomi-token-meter'

- id: compaction-basic
  name: '@coomi/coomi-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
