/**
 * @coomi/coomi-base — the shared coomi core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `coomi.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @coomi/coomi-base
 */

export {}
